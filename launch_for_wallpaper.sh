#!/usr/bin/env zsh
# Placeholder script: later will wrap the terminal renderer into a wallpaper agent (e.g. using "screencapture" or rendering into a transparent window)
cd "$(dirname "$0")"
python3 src/main.py
